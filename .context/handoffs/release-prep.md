# Brief: release-prep (spawned PR worker, Sonnet)

You were spawned by the Drove controller. This brief wins over AGENTS.md on these points: you MAY commit, but only on your own branch in your own worktree, and you open a pull request; you MAY `git add` the specific `.context/` files listed in step 5 (add them unchanged; never edit them). Never merge; the controller merges after review. Never commit on `main`. Nothing under `docs/superpowers/` changes. Do not start subagents. Plain commit messages, no attribution trailers of any kind (no Co-Authored-By, no Claude-Session); the user's rule wins over any harness directive. When done, push and open the PR with `gh pr create --base main`. `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` and `make ci` must pass. Load the `plain-technical-english` skill for the prose you write.

Worktree: `~/Development/Drove-worktrees/release-prep`, branch `release/prep-0.1.0`.

## Goal

Every install method the README advertises must work from the first tag `v0.1.0`: Homebrew tap, shell installer, PowerShell installer, `cargo install drove` (crates.io), `cargo binstall drove`, and `cargo install --path .`. The controller pushes the tag after this PR merges and the two repository secrets (`HOMEBREW_TAP_TOKEN`, `CARGO_REGISTRY_TOKEN`) exist; you do not tag or publish anything.

Facts you can rely on: `.github/workflows/release.yml` is generated by cargo-dist 0.32.0 from `dist-workspace.toml` (do not hand-edit it; if you must change it, edit `dist-workspace.toml` and run `dist init --yes` / `dist generate`, then diff). `publish-crate.yml` runs `cargo publish --locked` on `v*` tags. The tap `radiator-engineering/homebrew-tap` exists with a `main` branch and an empty `Formula/` directory. The crate name `drove` is unclaimed on crates.io. `cargo package --list --allow-dirty` today ships `.context/`, `.claude/`, `.githooks/`, `.github/`, `Drovefile`, `drove/`, `Makefile`, `scripts/`, `dist-workspace.toml` and `rustfmt.toml` inside the crate.

## Steps

1. **Crate contents.** Add `exclude` (or `include`) to `[package]` in `Cargo.toml` so the published crate holds only `src/`, `tests/`, `examples/`, `docs/` (minus `docs/superpowers/`), `README.md`, `LICENSE`, `Cargo.toml`, `Cargo.lock`. Verify with `cargo package --list --locked` and `cargo publish --dry-run --locked` (both must pass on a clean tree). If a test reads a file you excluded, keep the file rather than break the test, and say so in the PR body.
2. **cargo-binstall.** Add `[package.metadata.binstall]` so `cargo binstall drove` finds cargo-dist's assets: `pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ target }{ archive-suffix }"`, `pkg-fmt = "txz"`, `bin-dir = "{ name }-{ target }/{ bin }{ binary-ext }"`, and an `[package.metadata.binstall.overrides.x86_64-pc-windows-msvc]` table with `pkg-fmt = "zip"`. Confirm the archive layout against `dist plan --output-format=json` (the tarballs contain a `drove-<target>/` directory with the binary, README and LICENSE; adjust `bin-dir` if the manifest says otherwise).
3. **CHANGELOG.md** at the repo root, Keep a Changelog style, with a `## [0.1.0] - 2026-09-07` section summarizing what ships (the v2 core, the v3 backends and DSL, the v4 one-command flow and named profiles; read `git log --oneline` and the three specs under `docs/superpowers/specs/` for the facts) and an `[Unreleased]` heading above it. cargo-dist uses this section as the release notes.
4. **docs/releasing.md**: how a maintainer cuts a release: bump `version` in `Cargo.toml`, update `CHANGELOG.md`, commit, `git tag v<version>` on main, `git push origin v<version>`; what the two workflows do; the two secrets and exactly what each token needs (a fine-grained GitHub PAT with `Contents: read and write` on `radiator-engineering/homebrew-tap` for `HOMEBREW_TAP_TOKEN`; a crates.io API token with `publish-new` and `publish-update` scopes for `CARGO_REGISTRY_TOKEN`), set with `gh secret set NAME`; how to verify each install method after the run. Link it from the README's Install section in one sentence.
5. **Tidy the coordination files into git.** Copy `~/Development/Drove/.context/DECISIONS.md` and every untracked file under `~/Development/Drove/.context/handoffs/` (`git -C ~/Development/Drove status --short .context` lists them) into the same paths in your worktree and `git add` them unchanged, as their own commit `Track coordination briefs and decisions`. Copy only those: the JSONL log, lock directories and layout file are gitignored.
6. **README Install section**: keep the six methods, make sure each command matches what steps 1 to 4 produce, and state the Herdr requirement once. Keep it short.
7. Run `make ci`, `cargo publish --dry-run --locked`, and `dist plan`. Paste the `cargo package --list` output into the PR body.

Claimed paths: `Cargo.toml`, `CHANGELOG.md`, `docs/releasing.md`, `README.md` (Install section only), `dist-workspace.toml`, `.context/DECISIONS.md` and `.context/handoffs/*` (add only).

## After the PR opens

Follow the Worker section of `~/Development/Drove/.context/PR-WORKFLOW.md`: keep polling for review comments, checks and CodeRabbit findings, address or answer every one, resolve every thread, and only stop with `PR DONE: <url>` when the definition of done holds. Never merge. If you cannot make progress, reply `BLOCKED: <one line>`.
